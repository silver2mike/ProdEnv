pipeline {
  agent {label 'Ansible'}

  stages {
    stage('Hello') {
      steps {
        sh '''
          pwd
          ls -la
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
    }
  }
}
