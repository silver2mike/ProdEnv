pipeline {
  agent {label 'Ansible'}

  stages {
    stage('Hello') {
      steps {
        sh '''
          pwd
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
    }
  }
}
